[plugin]
name        = "border"
version     = "1.0.0"
kind        = "metadata"
entry       = "plugin:BorderPlugin"
api_version = "1.0"
schema      = ">=1.0,<2.0"

[requires]
python   = ">=3.11"
packages = []
plugins  = []

[ordering]
before = []
after  = ["speed"]
priority = 0

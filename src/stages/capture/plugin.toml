[plugin]
name        = "dual-video-file-capture"
version     = "1.0.0"
kind        = "capture"
entry       = "plugin:DualVideoFileCapture"
api_version = "1.0"
schema      = ">=1.0,<2.0"

[requires]
python   = ">=3.11"
packages = ["opencv-python>=4.9"]
plugins  = []

[ordering]
before = []
after  = []
priority = 100

[plugin]
name        = "axis-mapping-fusion"
version     = "1.0.0"
kind        = "fusion"
entry       = "plugin:Fusion"
api_version = "1.0"
schema      = ">=1.0,<2.0"

[requires]
python   = ">=3.11"
packages = []
plugins  = []

[ordering]
before = []
after  = []
priority = 100

[plugin]
name        = "route-plot"
version     = "1.0.0"
kind        = "exporter"
entry       = "plugin:PlotRouteExporter"
api_version = "1.0"
schema      = ">=1.0,<2.0"

[requires]
python   = ">=3.11"
packages = ["matplotlib>=3.8", "pandas>=2.0"]
plugins  = []

[ordering]
before = []
after  = []
priority = 100
